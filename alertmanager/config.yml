global:
  smtp_smarthost: 'smtp.gmail.com:587'
  smtp_from: 'rushikeshdl18@gmail.com'
  smtp_auth_username: 'rushikeshdl18@gmail.com'
  smtp_auth_password: 'bqlnilwuxhoajsll'   # the 16-char App Password, no spaces
  smtp_require_tls: true

route:
  receiver: 'default-email'
  group_by: ['alertname', 'app']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

  routes:
    - match:
        severity: critical
      receiver: 'default-email'
      group_wait: 10s
      repeat_interval: 1h

receivers:
  - name: 'default-email'
    email_configs:
      - to: 'rushikeshdl18@gmail.com,07.pandeyamit@gmail.com'
        send_resolved: true
        headers:
          subject: '{{ if eq .Status "firing" }}🔴 FIRING{{ else }}✅ RESOLVED{{ end }}: {{ .CommonLabels.alertname }} ({{ .CommonLabels.app }})'